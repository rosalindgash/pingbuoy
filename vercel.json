{
  "functions": {
    "src/app/api/metrics/*.ts": {
      "maxDuration": 30
    }
  }
  // NOTE: All monitoring jobs now run in Supabase Edge Functions via scheduled functions
  // No Vercel crons needed - everything runs on Supabase infrastructure
}